// Placeholder test kept so `dart test` can run repository-wide.
// Replace with real tests as needed.
import 'package:test/test.dart';

void main() {
	test('placeholder coordinate parser test', () {
		expect('a' + 'b', equals('ab'));
	});
}

